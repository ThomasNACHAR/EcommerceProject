<%= turbo_frame_tag "menu" do %>
  <section class="fixed top-0 left-0 w-full min-h-screen z-40">
    <%= link_to params[:original_url], data: {turbo_frame: "_top"} do %>
      <div class="inset-0 bg-black opacity-30 z-40 fixed cursor-pointer"></div>
    <% end %>
    <section class="w-[300px] relative lg:w-1/3 min-h-screen z-50 animation-menu">
      <div class="w-full z-50 bg-theme border-r-4 border-primary shadow-xl shadow-white min-h-screen">
        <div>yo !</div>
      </div>
    </section>
  </section>
<% end %>