<%= turbo_frame_tag "admin_dashboard" do %>
  <h1 class="font-bold md:text-6xl text-4xl text-center my-8 font-title">CATÉGORIES</h1>
  <%= turbo_frame_tag "categories_dashboard" do %>
    <div class="flex justify-center">
      <%= link_to "Créer une catégorie", new_admin_category_path, data: { turbo_frame: "categories_dashboard" }, class: "button" %>
    </div>
    <%= render partial:'admin/categories/categories', categories: @categories %>
  <% end %>
<% end %>
