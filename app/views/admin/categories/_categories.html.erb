<div class="flex flex-col justify-center items-center m-8 gap-8">
  <h2 class="font-bold text-3xl">Les catégories</h2>
  <% @categories.each do |category| %>
    <p><%= category.label %></p>
  <% end %>
</div>
